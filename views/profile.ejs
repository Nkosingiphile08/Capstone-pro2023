<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User Profile</title>
    <link rel="stylesheet" href="style.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-9ndCyUaIbzAi2FUVXJi0CjmCapSmO7SnpJef0486qhLnuZ2cdeRhO02iuK6FUUVM" crossorigin="anonymous">
    <style>
        .navbar-nav .nav-link .fa {
            color: #03a4ff;
        }

        body {
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
        }

        h1 {
            text-align: center;
        }

        .profile {
            backdrop-filter: blur(10px);
            background-color: rgba(45, 139, 253, 0.193);
            width: min(600px, 90vw);
            border: 1px solid #03a4ff;
            text-align: left;
            flex-direction: column;
            align-items: center;
            justify-content: space-evenly;
            margin: auto;
            border-radius: 5px;
        }
    </style>
</head>

<body>
   
    <section class="profile">

        <div class="container">
            <h1>Profile</h1>
            <div class="card" role="document">
                <div class="card">
                    <div class="card-header m-2 mb-2">
                        <h2 class="card-title text-lg-start">
                            Hi! <%= user.name %>
                        </h2>

                    </div>
                    <div class="card-body">
                        <!-- User Information Form -->
                        <p class="text-bg-info p-1 text-light">
                            Your Contact Info
                        </p>
                        <div>
                            <p>Contacts: <%=user.contacts%>
                            </p>
                            <p>Email: <%=user.email%>
                            </p>
                        </div>
                        <div>
                            <p class="text-bg-info p-1 text-light">
                                Active Appointment
                            </p>
                            <p>
                                <%=JSON.parse(user.appointments) ? JSON.parse(user.appointments).doctor : "" %>
                            </p>
                            <p>
                                <%=JSON.parse(user.appointments) ? JSON.parse(user.appointments).date : "" %>
                            </p>
                            <p>
                                <%=JSON.parse(user.appointments) ? JSON.parse(user.appointments).time : "" %>
                            </p>
                        </div>

                        <div class="container">
                            <h3>Book Appointment.</h3>
                            <a href="/appointment" class="btn btn-primary">Go to booking page</a>
                        </div>                      
                   </div>
                </div>
            </div>
        </div>
    </section>
    <br>

</body>

</html>